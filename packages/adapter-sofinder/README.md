# @soeditor/adapter-sofinder

Dependency-free adapter from an application-provided SoFinder picker to
SoEditor's generic `FileManager` capability. The host owns the concrete
SoFinder SDK/UI integration; no SoFinder runtime is pulled into SoEditor.
