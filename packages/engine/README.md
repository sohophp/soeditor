# @soeditor/engine

This package reserves the public boundary for a future editing engine. Phase 1
contains only the `EditingEngine` lifecycle interface and deliberately contains
no contenteditable, selection, or DOM synchronization implementation.
