{
    "name": "@soeditor/preview",
    "version": "0.5.0",
    "description": "Sandboxed configurable HTML preview for SoEditor.",
    "type": "module",
    "sideEffects": false,
    "files": [
        "dist"
    ],
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        }
    },
    "scripts": {
        "build": "vite build && tsc -p tsconfig.build.json",
        "test": "vitest run",
        "typecheck": "tsc -p tsconfig.json"
    },
    "peerDependencies": {
        "@soeditor/core": "workspace:*"
    },
    "devDependencies": {
        "@soeditor/core": "workspace:*"
    }
}
