# @soeditor/ui

Framework-independent DOM UI for SoEditor commands and contributions. It
provides configurable toolbars, accessible dialogs/notifications/status,
panels, balloons, theme variables, and host-scoped shortcuts. UI instances are
owned per editor and never mutate document state outside commands.
