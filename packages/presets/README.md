# @soeditor/presets

Immutable plugin and toolbar definitions for minimal, classic, developer, and
Markdown SoEditor configurations. Presets do not create editors or DOM surfaces
and do not hide Preview or FileManager application configuration.
