# @soeditor/rich-text

Independent command plugins for SoEditor's current visual schema: paragraphs,
headings, inline formatting, links, lists, blockquote/code, images, and basic
tables. Plugins delegate document mutation to the typed visual-editing service;
toolbar and host code invoke the same commands.
