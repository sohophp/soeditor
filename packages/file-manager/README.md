# @soeditor/file-manager

Replaceable file-selection capability and command-driven Image integration for
SoEditor. Applications register a `FileManager` using
`fileManagerServiceToken`; `FileManagerPlugin` adds `image.browse` without
changing the underlying Image plugin.
