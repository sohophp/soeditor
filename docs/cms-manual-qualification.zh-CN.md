# CMS 真实设备与素材验收

当前全部人工项 **待执行**；尚无真实 Safari、Office、操作系统输入法或屏幕阅读器结果。不得用自动化通过替换本表状态。

## 启动和记录

```bash
pnpm --filter @soeditor/playground dev --host 0.0.0.0 --port 4173
```

开发脚本绑定 `0.0.0.0`。WSL 对应 Windows 地址为 `http://localhost:4173/classic.html?test=1`；示例无需账号密码。其它设备使用宿主实际局域网地址的相同路径，是否可达取决于网络配置。这里未声称已经启动常驻服务。

每次记录：操作人、日期、OS/浏览器/输入法/Office/辅助技术版本、工作区快照 SHA-256、样本来源及脱敏情况、操作步骤、预期与实际、保存后 HTML、通过/失败及问题编号。不要只写“看起来正常”。

| 项目                  | 执行步骤与验收                                                                                                                       | 状态   |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| Windows 中文输入法    | 微软拼音输入候选、翻页、取消、标点、连续两次组合；分别在段落、列表、单元格执行；一次组合可完整撤销，无重复字符或丢字；输入后立即保存 | 待执行 |
| Word 真实粘贴         | 用真实文章复制标题、链接、嵌套列表、脚注文本和图片；粘贴后检查语义、清理结果、撤销、保存重开；记录不支持的素材，不静默丢弃           | 待执行 |
| Excel 真实粘贴        | 复制含表头、合并单元格、中文、换行的区域；检查表格结构、内容、单元格导航及保存结果，不要求导入公式或电子表格功能                     | 待执行 |
| Safari macOS/iOS      | 原生选区、拖动、链接/图片/表格对话框、取消后焦点、Source 首次/再次/两种分屏、只读、立即保存；iOS 补软键盘与页面滚动                  | 待执行 |
| NVDA + Firefox/Chrome | 键盘进入工具栏及菜单、读出名称与状态、打开/取消对话框、错误提示、返回编辑区；确认无焦点陷阱                                          | 待执行 |
| VoiceOver + Safari    | 同上；另验证 Source 编辑区、分屏分隔条、上传失败/重试通知可理解且不重复播报                                                          | 待执行 |
| 真实 CMS 文章         | 选择脱敏历史文章和产品表格，打开 → 修改指定段落 → Source 往返 → 保存 → 重新打开；未修改的注释、类、属性、未知元素及占位符语义不变    | 待执行 |
| 长时间编辑            | 连续 60 分钟输入、菜单、上传取消重试、只读/重置、分屏，多实例交替；记录失焦、卡顿、内容差异、内存趋势和复现步骤                      | 待执行 |

## 结果模板

```text
项目：
执行人／日期：
环境版本：
工作区快照／构建：
样本来源与脱敏：
步骤：
预期：
实际与保存后 HTML：
结果（通过／失败／未执行）：
证据路径与问题编号：
```

脚本或历史文件包含的“通过”不是本轮人工签署。若发现内容丢失、危险内容执行、保存或撤销错误，保留最小复现并先修复再重跑对应流程。
