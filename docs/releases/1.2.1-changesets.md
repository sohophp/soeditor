# Applied 1.2.1 changeset

The patch below was applied to the aligned 1.2.1 package manifests.

```md
---
'@soeditor/editor': patch
---

Keep object properties stable across independently minified chunks in the CMS ESM build to prevent HTML tokenizer initialization hangs. Add an actual-artifact browser regression.
```
