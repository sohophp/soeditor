# CMS 实用 UI/UX 改进与验收

用户目标：按顺序完成 2026-09-06 比较建议的全部范围。保留现有未提交改动，不发布或部署。九项工作已完成实现与约定自动验收；最终结果见文末，阶段中的失败和待办文字保留为历史过程。

| 顺序 | 工作                           | 验收证据                                                                                   | 状态   |
| ---- | ------------------------------ | ------------------------------------------------------------------------------------------ | ------ |
| 1    | 列表操作范围与连续键盘行为     | 单项、多项、嵌套、跨列表、Enter/Backspace/Tab、撤销；保留前后项目、编号和 CMS 属性         | 已完成 |
| 2    | 格式统一值、混合值、不可用状态 | 对齐、字号、颜色、列表；CSS 继承、多段选择、只读、取消选择                                 | 已完成 |
| 3    | 轻量与响应                     | 冻结 JS/CSS/初始请求预算通过；菜单首次打开、连续输入和生命周期测量                         | 已完成 |
| 4    | 工具栏可读性与分组             | 文字/段落/插入分组、标题字号可读值、低频入口；实际点击数                                   | 已完成 |
| 5    | 统一菜单规则                   | 当前状态、焦点、箭头、间距、关闭/返回焦点、上下/左右视口边界                               | 已完成 |
| 6    | 图片与表格上下文工具           | 常用操作首层、详细属性面板；避开编辑位置及拖拽手柄                                         | 已完成 |
| 7    | 粘贴与上传反馈                 | Word/网页粘贴；上传中、失败、重试、取消；就近错误和内容保留                                | 已完成 |
| 8    | 窄屏与无障碍                   | 375px、200% 缩放、触屏目标、键盘、高对比度、名称和快捷键                                   | 已完成 |
| 9    | 完整 CMS 连续工作流与总验收    | 粘贴→标题/列表→图片/表格→Source→撤销/重做→表单提交；构建/类型/lint/单元/浏览器/消费者/体积 | 已完成 |

自动浏览器与实际 Safari、辅助技术人工验收分别记录，不以自动结果声称人工认证。界面可以借鉴公开行为，使用 SoEditor 自身命令和事务，HTML 保真与不执行危险内容的契约保持不变。

## 阶段 1 证据（2026-09-06）

- `packages/wysiwyg/src/list-editing.ts`：按实际文本选区选择项目，排除结束位置为下一项开头的未选项目；分段转换、取消列表保留后续 `start`、`reversed`、显式 `li[value]` 和 CMS 属性，列表 ID 不重复。
- 缩进处理支持列表项内段落；批量减少缩进保持项目次序，提升项目时携带后续嵌套内容，保留阅读顺序。删除旧的整列表取消路径和不再使用的私有选区转换方法。
- Chromium 专项 6 项通过（8.8 秒）：单项/多项/嵌套/跨列表、正序与倒序编号、Tab/Shift+Tab、Enter、空项退出、Backspace 合并、撤销、表格选择禁用状态、现有样式菜单。
- `pnpm --filter @soeditor/wysiwyg build`、同包 `typecheck`、相关 TypeScript 与浏览器用例 ESLint 通过。浏览器场景库存由 244 增至 247；完整工作区构建与冻结体积门禁留待后续阶段，当前未声明通过。

下一阶段：统一读取当前格式，处理 CSS 继承、多段混合值和无可用选区。标题/字号/颜色/对齐/列表控件共用统一状态规则，避免增加默认依赖或在每次状态刷新中反复扫描整篇文档。

## 阶段 2 证据（2026-09-06）

- 编辑服务新增可选 `getFormatStates()`，以 `uniform`、`mixed`、`unavailable` 表达对齐、标题、列表、字号、字体、文字色、背景色和高亮；读取渲染样式与 CSS 继承，不改变选区或 HTML。
- Classic 使用显式 `readFormatStates` 回调连接引擎与 UI；每次 UI 刷新缓存一份结果。独立 UI 和兼容引擎仍可省略此能力。
- 菜单读取当前值、取消混合选择下的错误高亮，提供本地化状态提示；颜色面板打开时读取当前颜色，字号/字体/颜色预设显示选中状态。
- 清空作者选区时清除过期格式；菜单/对话框保留选区的现有机制保留。表格单元格 pointerdown 优先读取当前单元格，避免沿用之前的列表状态。
- Chromium 组合专项 6 项通过（9.1 秒），涵盖 CSS 类继承、多段混合、应用与撤销、字号/字体/文字色/背景/高亮/标题状态、只读、清空选区、列表菜单与表格状态。
- UI 与 WYSIWYG 构建/类型检查、Classic 宿主类型检查、相关 ESLint 通过。完整冻结体积与加载性能检查是下一阶段，不将阶段 1/2 的功能通过等同于发布通过。

下一阶段：在当前完整构建上重新测量全局包、CSS、可选 CMS 初始请求图和交互延迟；通过消除重复或按需隔离降低体积，不提高预算、不删除已支持的普通 CMS 功能。

## 阶段 3 进行中：重复代码与资源隔离（2026-09-06）

- 全局构建沿用 `SOEDITOR_TABLE_CONTEXT=false`，将未启用的表格上下文专属翻译放到同一编译条件下；每种中文资源隔离 59 项。完整 ESM 构建的全部翻译对象与修改前快照逐项深比较一致。
- 表格、行、单元格的兼容属性对话框共用字段创建与提交逻辑，保留字段顺序、空值/省略规则、数值转换、行高限制与自定义属性验证。没有删除入口或增加运行时依赖。
- 当前全局构建：JS 原始 496,673 字节、gzip 152,640 字节，CSS 原始 29,081 字节。原始 JS 满足 500,000 上限；gzip 仍超 2,640 字节，CSS 仍超 2,081 字节，预算未调整，阶段 3 尚未通过。
- UI 构建（含声明类型检查）、相关 ESLint、8 项 UI 单元测试通过；Chromium 全局分发 3 项通过（4.4 秒），覆盖自包含加载、Source 隔离、压缩后的图片缩放与表格段落控件。这些测试不替代全部属性对话框及 CMS 连续流程验收。
- 本轮日志：`/tmp/soeditor-ux-stage3-global2.log`、`/tmp/soeditor-ux-stage3-ui-refactor.log`、`/tmp/soeditor-ux-stage3-ui-tests.log`、`/tmp/soeditor-ux-stage3-lint.log`、`/tmp/soeditor-ux-stage3-distribution.log`；数值快照 `/tmp/soeditor-ux-stage3-measurement.json`。临时日志仅供本地追踪，最终证据需随总验收归档。

下一步仍为阶段 3：压缩重复样式和代码，完成冻结体积及交互测量，然后依次推进阶段 4–9。

## 阶段 3 专项证据：CMS 表格渲染边界与冻结预算（2026-09-06）

- 将原生表格命令/服务抽为 `CmsTablePlugin`，CMS 与 CMS runtime 预设使用它；`TablePlugin` 继承相同命令并保留旧结构化引擎的节点视图，classic 兼容预设不变。最终方案同时适用于 ESM 与全局版，已移除中间的全局专用节点视图编译开关。两插件共用 `table` ID，每实例选择一个。
- 全局 CSS 按已经存在的功能边界排除旧投影占位样式、未启用表格上下文的行列缩放样式；完整 ESM 样式仍保留。没有移除原生内容编辑、HTML 保留、表格命令或兼容入口。
- 完整 `pnpm build` 通过；富文本 51 项单元测试、预设 4 项单元测试、相关 ESLint 和 diff 检查通过。Chromium 19 项通过（9.3 秒），覆盖原生表格选择/合并/拆分/属性/标题/行列/撤销、旧结构化表格以及压缩后全局编辑、保存和清理。
- 冻结门禁全部通过：[加载证据](evidence/cms-ux-2026-09-06/stage3-loading.json)。全局 JS 471,937 字节 / gzip 145,146 字节；CSS 26,497 字节；含三个菜单的可选 CMS 初始 JS 499,644 字节 / gzip 148,367 字节。Source、格式化和恢复资源按需边界通过；没有提高任何预算。
- 10/100/500 KiB 的 Source 首次进入约 187/353/773 ms，再次进入约 30/163/450 ms，均在既定预算内。每种规模同时覆盖启用与未启用 Source，分别完成 3 次生命周期循环。
- 三个菜单首次打开到下一帧约 4–41 ms。[连续输入证据](evidence/cms-ux-2026-09-06/stage3-continuous-input.json)：每种规模输入 12 个连续字符，验证未丢失字符；剔除前 2 次后中位延迟约 13/47/247 ms。500 KiB 文档输入仍偏慢，未声称大文档输入性能达成额外低延迟标准，也不将这些本地 Chromium 结果等同于全部浏览器认证。

下一阶段：工具栏的标题/字号可读值、文字/段落/插入分组和低频操作入口；后续界面改动需继续满足本阶段冻结预算。

## 阶段 4 专项证据：可读值、分组与低频操作（2026-09-06）

- 标题和字号菜单直接显示当前值；标题读取语义层级，字号读取渲染值。混合选区显示本地化的“混合 / Mixed”，无有效标题选区显示通用“标题”入口；完整状态保留在提示和无障碍描述中。
- CMS 与 CMS runtime 工具栏按文字、段落、插入分组。删除线、上下标、清除格式集中到“更多文字格式”；原有独立工具栏贡献仍可显式配置，未删除命令或入口能力。
- 实际浏览器操作路径：粗体/斜体/下划线 1 次点击；删除线/上下标/清除格式 2 次（菜单 + 操作）；标题/字号应用 2 次，读取当前值 0 次。正文、嵌套列表和表格内容均验证原选区命令行为。
- 375px 截图检查发现并修复新菜单向右越界：与对齐/列表复用水平定位并响应窗口尺寸变化，销毁时清理监听器。断言同时检查文字控件及弹出菜单边界，不仅检查可见性。见 [375px](evidence/cms-ux-2026-09-06/stage4-toolbar-375.png) 和 [1280px](evidence/cms-ux-2026-09-06/stage4-toolbar-1280.png)。
- UI 与预设构建/类型声明、相关 ESLint、8 项 UI 和 4 项预设单元测试通过；12 项组合浏览器回归、2 项定位/图库复测、最终文字值与内联操作复测、中文 CMS 展示页复测通过。验证中文删除线/下标标签，混合值、选区、375px 与宽屏。没有新增重复场景计数。
- [本阶段加载证据](evidence/cms-ux-2026-09-06/stage4-loading.json) 的冻结门禁通过：全局 JS 474,066 字节 / gzip 145,686 字节；CSS 26,842 字节；可选 CMS 初始 JS 501,651 字节 / gzip 148,833 字节。Source 首次/再次进入、按需资源和生命周期继续通过。CSS 余量仅 158 字节，后续菜单样式必须合并重复规则，不能抬高预算。

下一阶段：统一全部菜单的状态、键盘导航、焦点返回、关闭规则与上下/左右视口边界。第四阶段的新菜单水平边界修复不替代第五阶段的全菜单验收。

## 阶段 5 专项证据：统一菜单与弹层（2026-09-06）

- 工具栏统一绑定菜单控制器，覆盖标题、字号、字体、更多文字格式、对齐、列表、颜色及图片入口；删除列表/特殊字符菜单的重复定位和图库独立键盘分支。补齐下拉箭头、文本菜单选中态与高对比度选中轮廓。
- 上下键打开菜单；列表与网格内方向键/Home/End 保持在菜单中；输入框保留原生编辑键；Escape 关闭并回到入口。不可用菜单不展开，切为只读时关闭已打开菜单。一次只保留一个工具栏菜单。
- 菜单自动选择上下空间并限制左右/高度，空间不足时内部滚动。响应窗口/滚动、锚点和面板尺寸变化；重定位不清空滚动位置，聚焦末项在重定位后仍可见。销毁清理事件和 ResizeObserver。
- 表格尺寸弹层在异步精确输入区出现后重新定位；输入框支持 Escape 回入口，外部关闭后的下一次点击立即重开。通用弹层限制高度并清理观察器，关闭同步入口展开状态。
- 将作者选区高亮置于内容上方、菜单下方，修复向上菜单文字被高亮遮盖。[截图](evidence/cms-ux-2026-09-06/stage5-menu-above.png)。
- 新浏览器场景覆盖 11 种菜单在 375px 边缘的导航/边界，动态内容增高、重定位后的焦点可见性、颜色输入原生方向键、360px 高度下表格输入框退出/重开、外部关闭与只读。场景库存由 249 增至 250。
- 最终组合回归 10 项通过（12 秒），观察器/颜色/重复弹层复测 4 项通过，最终滚动焦点测试通过；UI 构建和声明类型检查、8 项单元测试、相关 ESLint、文档审计与 diff 检查通过。颜色回归明确验证关闭不改内容、重开读取已应用颜色，再次输入应用到原选区，不依赖未应用草稿保留。
- [加载门禁](evidence/cms-ux-2026-09-06/stage5-loading.json) 全部通过：全局 JS 476,434 字节 / gzip 146,124 字节，CSS 26,910 字节；可选 CMS 初始 JS 504,021 字节 / gzip 149,480 字节。Source、格式化、恢复、生命周期门禁保持通过，未提高预算。后续总验收仍需覆盖整个编辑工作流及其他浏览器限制。

下一阶段：图片与表格上下文工具的常用/高级层次，以及编辑位置和拖拽手柄避让。

## 阶段 6 专项证据：图片快捷操作与上下文避让（2026-09-06）

- 图片选择后显示左/中/右对齐与属性入口；详细属性继续分组，响应式设置默认折叠。拖拽时关闭快捷工具，结束后恢复；使用引擎更新与撤销，不从 UI 直接修改保存内容。
- 浮动工具支持实时避让矩形；图片提供八个缩放手柄，表格提供当前编辑单元格。候选位置限制在视口内，选择重叠面积最小的位置；极小视口无空位时不承诺完全无重叠。每个编辑器保留一个上下文分支，嵌套弹层保留父层。
- 修复文字中间图片对齐生成段落内 figure 的问题：拆分段落与内联祖先，保留前后文字、业务类名和唯一 ID；撤销还原原结构。
- Chromium 上下文组合 6 项通过（8.9 秒），覆盖图片属性、缩放取消/只读/Source 替换、快捷对齐/撤销、表格菜单及多实例弹层隔离。375px 截图与几何断言确认工具避开当前单元格：[截图](evidence/cms-ux-2026-09-06/stage6-table-avoid.png)。压缩全局分发 3 项通过（5.4 秒），新增实际点击图片对齐并撤销，验证属性压缩后的行为。
- 完整工作区构建通过，末次图片结构修复后 WYSIWYG 重新构建/类型检查及全局重新构建通过；UI 单元测试 8 项、相关 ESLint 通过。新增 `BalloonOptions.avoid` 已记录 UI README 与 API 报告。完整总回归仍在阶段 9。
- [加载门禁](evidence/cms-ux-2026-09-06/stage6-loading.json) 全部通过：全局 JS 478,885 字节 / gzip 146,879 字节，CSS 26,910 字节；未提高预算。Source、格式化、恢复和生命周期按需边界继续通过。

下一阶段：粘贴与上传的作者可见反馈。已有上传服务测试不能替代真实界面的进度、失败、重试和取消操作验收。

## 阶段 7 专项证据：上传反馈与粘贴拒绝（2026-09-06）

- `UploadPlugin` 通过状态贡献显示文件名、原生进度条、失败原因及取消/重试按钮；完成或取消后可关闭反馈。任务行保持原节点，进度变化不丢键盘焦点；只读禁用重试。区域限制高度并内部滚动，不向正文写入状态或临时 URL。
- 提供英文、简繁中文文案。真实按钮回归覆盖失败后重试成功、关闭、取消后正文不变、50% 进度和焦点保持、只读切换。中文 375px 验证：[截图](evidence/cms-ux-2026-09-06/stage7-upload-375.png)。
- 粘贴图片整批同步校验类型、大小和并发数，再启动任务；修复预检查失败被异步吞掉而静默消耗粘贴的问题。浏览器验证五张图片超过并发限制时显示错误、没有部分创建任务、原正文不变。
- Chromium 最终组合 3 项通过（6.9 秒）：上传完整路径，Office 外部清理与内部剪贴板保真，表格内富内容粘贴及一次撤销；没有将合成剪贴板测试称为真实 Word 应用人工兼容认证。文件管理 25 项单元测试通过，完整工作区构建通过；末次文件管理修复重新构建及类型声明通过。
- [加载证据](evidence/cms-ux-2026-09-06/stage7-loading.json) 冻结门禁通过：全局 JS 479,171 字节 / gzip 146,974 字节，CSS 26,910 字节。上传 UI 位于已安装 UploadPlugin 的集成路径，不新增默认 CMS runtime 上传依赖。最后任务区高度限制不影响该默认加载图；完整集成构建与总回归仍由阶段 9 复验。

下一阶段：窄屏、200% 缩放、触屏、键盘、高对比度与可访问名称的组合验收。

## 阶段 8 专项证据：缩放、触屏与无障碍（2026-09-06）

- 将现有 360px 高对比度测试从 150% CSS zoom 提升为 200%，补测对齐菜单完整视口边界、End 导航、Escape 关闭与入口焦点恢复；Chromium 通过（6.4 秒）。CSS zoom 模拟不等同于人工浏览器缩放认证。
- RTL 窄屏 CMS 测试加入对齐、有序和符号列表菜单：桌面实际点击，触屏项目实际 tap；每种样式选项至少 44×44px，应用后菜单关闭。保留帮助窗口返回焦点、触屏高度调整及 WCAG 2 A/AA axe 扫描。
- 桌面/触屏 Chromium 共 6 项通过（8.8 秒），另涵盖独立中文组合输入与 CMS 提交/清理。第 4–7 阶段的 375px 菜单边界和上传截图作为关联证据；本阶段只扩展现有场景，没有增加库存计数。相关 ESLint 通过。
- Firefox 与 WebKit 已尝试运行，但全部停在浏览器启动阶段：Firefox 的宿主 `/lib64/libstdc++.so.6` 缺少 `GLIBCXX_3.4.26`；WebKit 缺少 GTK 4、Vulkan、Graphene 等共享库。未将这六次启动失败视为应用断言失败或跨浏览器通过。真实 Safari 与辅助技术人工验收仍未完成。

下一阶段：完整工作区 lint、类型、测试、构建及连续作者工作流总验收。宿主浏览器依赖限制单独记录，不能由 Chromium 成功替代。

## 阶段 9 进行中：连续流程与工作区门禁（2026-09-06）

- 新增不重设正文的作者流程：粘贴文章、设置标题与列表、上传图片、插入表格、Source 追加标记、撤销/重做、表单提交。桌面与触屏 Chromium 2 项通过（6.1 秒），最终提交逐项验证标题、列表、图片、表格和 Source 标记均保留。场景库存由 252 增至 253。
- `pnpm typecheck` 完整通过。首次 `pnpm lint` 的 ESLint 通过，Prettier 报告六份新证据 JSON 格式问题；格式化后完整 `pnpm lint` 通过，没有修改证据中的测量值。
- 完整 `pnpm test` 已启动，尚未得到全链结果。本阶段和整个目标仍未完成。

- 第一轮完整测试链已通过全部单元测试及集成性能预算，在 API 摘要检查处停止；更新差异只有文件管理声明树 SHA-256（新增内部上传状态模块），公开导出没有变化。API 与文档审计复核通过，已重新运行完整测试链。
- 通过已有 `mcr.microsoft.com/playwright:v1.62.1-noble` 容器绕过宿主共享库缺失，Firefox/WebKit 四项 CMS 场景各自通过，共 8 项（25.9 秒）：连续文章、中文组合输入、RTL/触屏尺寸/axe、提交与安全/销毁。[运行结果](evidence/cms-ux-2026-09-06/stage9-cross-browser.txt)，[容器配置](evidence/cms-ux-2026-09-06/stage9-cross-browser.config.mjs)。容器使用独立网络和 Node 24 启动 Vite 测试服务器；工作区构建/类型/测试主链仍使用宿主 Node 22。未修改宿主库，也未将 WebKit 自动化称为真实 Safari 或屏幕阅读器人工认证。
- Firefox 首轮暴露两处测试可移植性：合成 ClipboardEvent 需要显式 clipboardData，44px 布局返回 43.999969 的浮点值。已与既有粘贴用例一致显式提供数据，并将几何读数保留两位小数后检查 44px；内容与尺寸要求未降低。

- 第二轮完整链已通过 API、文档、打包消费者、插件兼容、24 包分发与发布产物审计；135 项 Chromium 回归中 130 项通过、5 项失败。一个列表预期未包含应保留的 `type="A"`，已更新；另外四项暴露弹层被容器裁切的真实问题。
- 修复工具栏菜单使用原生 popover 顶层显示并按 CSS zoom 换算坐标；紧凑表格浮层不再用滚动容器裁切嵌套菜单，仅超高内容内部滚动。失败项与 200% 高对比度、全菜单视口组合共 7 项复测通过（14.4 秒）。这些运行时修复仍需完整构建、冻结预算和全链复验，当前不能据专项通过结束目标。

## 最终验收（2026-09-06）

九项工作完成；以上“进行中”“待复验”均为历史阶段记录，以本节为最终状态。

- **完整 `pnpm test` 退出码 0**：[验收摘要](evidence/cms-ux-2026-09-06/stage9-validation.txt)。包含 403 项单元测试、集成性能门禁、完整工作区构建/API/文档审计、打包消费者和插件兼容、24 包分发/发布产物审计、135 项 Chromium 产品回归（37.2 秒）、8 项桌面/触屏 CMS 测试（10.6 秒）。发布审计不代表已经发布或部署。
- 完整 `pnpm lint`、工作区 `pnpm typecheck` 通过；最后修改只涉及 UI 运行时后，UI 类型检查再次通过。构建中的声明类型检查覆盖最终源码，`git diff --check` 通过。
- 最终弹层修复后 Firefox/WebKit 在 Playwright 1.62.1 Noble 容器中再次通过 8 项 CMS 场景（26.2 秒）：[结果](evidence/cms-ux-2026-09-06/stage9-cross-browser-after-overlays.txt)。
- **最终冻结加载门禁全部通过**：[原始测量](evidence/cms-ux-2026-09-06/stage9-final-loading.json)。全局 JS 479,710 字节 / gzip 147,133 字节；CSS 26,910 字节；可选 CMS 初始 JS 505,472 字节 / gzip 149,989 字节。未改变预算上限。
- 10/100/500 KiB Source 首次进入约 226/426/723 ms，再次进入约 27/123/440 ms；Source、格式化和恢复资源按需边界、菜单交互、输入/粘贴/表格及重复创建销毁门禁通过。
- 已保留用户原有未提交工作；没有创建发布、部署或提交。最终变更包括对齐/列表预览及操作范围、格式状态、工具栏分组、统一菜单、图片/表格工具、上传反馈、窄屏键盘可访问性和连续作者流程。

验收边界：375px 与 200% CSS zoom、合成剪贴板和 axe 为自动化证据，不等同于真实 Word、Safari 或屏幕阅读器人工认证。500 KiB 文档此前连续输入中位约 247 ms，仍是已记录的大文档性能限制；本次通过的是既定冻结预算，没有声明额外低延迟保证。
